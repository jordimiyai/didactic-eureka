{
  "scripts": {
    "start": "concurrently \" cd ./api-wallets && npm run start:dev\"  \"cd ./client && npm run start\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "concurrently": "^7.1.0"
  }
}